# Preview channel available

Want to try unreleased changes? Switch to the preview branch:

    git checkout preview
    # or for a fresh clone:
    git clone -b preview https://github.com/KnudsenMorten/SecurityInsight.git

Preview is updated whenever a -preview tag is cut upstream. It may include
features that don't make it into the next stable release.
