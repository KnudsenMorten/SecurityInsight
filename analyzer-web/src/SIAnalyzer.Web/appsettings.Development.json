{
  "//": [
    "LOCAL DEVELOPMENT ONLY. ASP.NET Core loads this file only when",
    "ASPNETCORE_ENVIRONMENT=Development, so it never applies to the hosted deployment.",
    "",
    "SIAnalyzer:Auth:RequireClientPrincipal defaults to TRUE (fail-closed, operator",
    "directive 2026-08-05: authenticated by default). A local `dotnet run` has no platform",
    "Easy Auth in front of it, so no X-MS-CLIENT-PRINCIPAL header is ever injected and every",
    "request would 401. Turning the gate off HERE keeps local/demo runs usable without",
    "weakening the deployed app -- the previous arrangement had the default OFF everywhere,",
    "and Deploy-SIAnalyzer.ps1 never turned it on, so production was unprotected."
  ],
  "SIAnalyzer": {
    "Auth": {
      "RequireClientPrincipal": false
    }
  }
}
