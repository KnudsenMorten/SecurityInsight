{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//SIAnalyzer": "Internal env is the DEFAULT base: in the hosted internal deployment set SIAnalyzer:WorkspaceId to the internal SI workspace customerId (GUID), SIAnalyzer:OpenAiEndpoint + SIAnalyzer:OpenAiDeployment to the SI Azure OpenAI deployment (AI on by default). Leave WorkspaceId empty for the demo fallback. NO secrets/IDs are committed here - the host (App Service / Container App settings, Key-Vault-backed) supplies them.",
  "SIAnalyzer": {
    "WorkspaceId": "",
    "UseDemoData": false,
    "OpenAiEndpoint": "",
    "OpenAiDeployment": "",
    "OpenAiApiKey": "",
    "TimelineLookbackDays": 180
  }
}
