# SecurityInsight_CriticalAssetTagging_Custom.yaml
# -------------------------------------------------
# YOUR customer-owned overrides. This file is preserved across Step 0 updates.
# Anything you add here merges on TOP of the Locked catalog (by AssetTagName stem).
#
# Add a rule here when you need to:
#   * Override the Mode of a shipped Locked rule (Test -> Prod, or vice versa).
#   * Tighten / loosen the KQL of a shipped Locked rule.
#   * Add an entirely new tagging rule the Locked catalog doesn't cover.
#
# Same stem as a Locked rule -> YOUR rule WINS. Different stem -> both apply.

AssetTagging: []